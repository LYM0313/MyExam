#define _CRS_SECURE_NO_WARNINGS

#include<stdio.h>
#include<string.h>

int main() {
	int a, b, c;

	scanf_s("%d %d %d", &a, &b, &c);

	printf("%d\n", (a + b) % c);
	printf("%d\n", ((a % c) + (b % c)) % c);
	printf("%d\n", (a * b) % c);
	printf("%d\n", ((a % c) * (b % c)) % c);

	return 0;
}
